# import Post from "./generated/prisma.graphql"

type Query {
  me: User
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
}

type AuthPayload {
  token: String!
  user: User  
}

type User {
  uid: String 
  displayName: String
  email: String
}
